{
  "name": "@runner-center/controller",
  "version": "0.2.0-rc.3",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && go build -o dist/runner-center-controller ./cmd/runner-center-controller",
    "test": "go test ./...",
    "typecheck": "go vet ./..."
  }
}
