{
  "name": "@runner-center/release",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "package-agent": "node --experimental-strip-types bin/package-agent.ts",
    "test": "node --test --experimental-strip-types \"tests/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@runner-center/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
