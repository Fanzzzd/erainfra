{
  "name": "@erainfra/release",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build-go-assets": "node --experimental-strip-types bin/build-go-assets.ts",
    "package-agent": "node --experimental-strip-types bin/package-agent.ts",
    "test": "node --test --experimental-strip-types \"tests/**/*.test.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@erainfra/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
